#include <bits/stdc++.h>
using namespace std;

int main() {
  http://127.0.0.1/titip-absen/
  ios_base::sync_with_stdio(0);
  int n;
  vector<int> lamp, current;
  cin >> n;
  lamp.resize(n);
  current.resize(n);
  for (int i = 0; i < n; i++) {
    cin >> lamp[i];
    current[i] = lamp[i];
  }
  int t = 1;
  for (int i = 0; i < n; i++) {
    for (int j = 0; j < n; j++) {
      current[i]--;
      if (current[i] = 0
    }
  }
  cout << t << "\n";
  return 0;
}
