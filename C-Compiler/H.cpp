#include <bits/stdc++.h>
using namespace std;

int main() {
  http://127.0.0.1/titip-absen/
  ios_base::sync_with_stdio(0);
  long long n, b;
  cin >> n >> b;
  if (b == 0)
    cout << "YA\n";
  else if (n >= pow(2,b))
    cout << "YA\n";
  else
    cout << "TIDAK\n";
  return 0;
}
